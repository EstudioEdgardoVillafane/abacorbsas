import { UpperFirstCaracterPipe } from './upper-first-caracter.pipe';

describe('UpperFirstCaracterPipe', () => {
  it('create an instance', () => {
    const pipe = new UpperFirstCaracterPipe();
    expect(pipe).toBeTruthy();
  });
});
